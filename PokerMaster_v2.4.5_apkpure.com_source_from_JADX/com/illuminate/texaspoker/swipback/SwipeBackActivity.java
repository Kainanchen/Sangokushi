package com.illuminate.texaspoker.swipback;

import a.does.not.Exists2;
import android.os.Build.VERSION;
import android.os.Bundle;
import android.support.v7.app.AppCompatActivity;
import android.view.View;
import com.ali.fixHelper;

/* renamed from: com.illuminate.texaspoker.swipback.a */
public class SwipeBackActivity extends AppCompatActivity {
    private SwipeBackActivityHelper f3521a;

    static {
        fixHelper.fixfunc(new int[]{565, 566, 567, 568});
        if (VERSION.SDK_INT <= 0) {
            Exists2.class.toString();
        }
    }

    public native View findViewById(int i);

    public native void onCreate(Bundle bundle);

    protected native void onPostCreate(Bundle bundle);
}
